/**
 * The default algorithm used when calculating the integrity of an asset.
 */
export const DefaultIntegrityAlgorithm = "sha512";

/**
 * The default length of the integrity hash when included in a file name.
 */
export const DefaultFileNameHashLength = 32;
